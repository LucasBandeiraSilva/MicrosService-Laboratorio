spring:
  application:
    name: msresultados
  kafka:
    bootstrap-servers: 'localhost:29092'
    consumer:
      group-id: healthflow-resultados


healthflow:
  config:
    kafka:
      topics:
        pedidos-faturados: 'health_flow.exames-faturados'
        pedidos-prontos: 'health_flow.exames-prontos'

server:
  port: 8085